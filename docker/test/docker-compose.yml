misasdb_test:
  extends:
    file: ../common/docker-compose.yml
    service: misasdb
  # We do not persist data on test
  # volumes:
  # 	- ../../data/runtime/db:/data/db
  # 	- ../../data/dump:/dump

misas_test:
  build: content
  links:
    - misasdb_test
  environment:
    - MONGO_URL=mongodb://misasdb_test/misas
    - ROOT_URL=http://localhost:80
  ports:
    - 3101:80
